rootProject.name = "dokka"

include 'core',
        'integration',
        'runners:fatjar',
        'runners:ant',
        'runners:maven-plugin',
        'runners:gradle-plugin',
        'runners:gradle-integration-tests'
